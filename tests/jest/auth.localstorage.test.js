/**
 * Tests stockage liés à l’auth historique supprimés — la persistance activation passe par OPFS/auth.js en navigateur.
 */
describe('auth storage (legacy removed)', () => {
  test('placeholder — voir license-activation.test.js', () => {
    expect(true).toBe(true);
  });
});
